#ifndef READ_FILE_H
#define READ_FILE_H

/**
 * @brief Lee y muestra el contenido de un archivo especificado por el usuario.
 *
 * Esta función permite al usuario ingresar el nombre de un archivo, luego lo abre en modo de lectura 
 * y muestra su contenido en la consola. Si el archivo no puede ser abierto, se notifica al usuario.
 */
void readFile();

#endif // READ_FILE_H
