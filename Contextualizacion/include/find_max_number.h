#ifndef FIND_MAX_NUMBER_H
#define FIND_MAX_NUMBER_H

/**
 * @brief Encuentra el número más grande en un archivo que contiene una lista de números.
 *
 * Esta función abre un archivo especificado por el usuario, que contiene una lista de números enteros,
 * y recorre el archivo para encontrar y devolver el número más grande. Si el archivo no se puede abrir,
 * se imprime un mensaje de error y la función retorna sin realizar la búsqueda.
 */
void findMaxNumberInFile();

#endif // FIND_MAX_NUMBER_H
