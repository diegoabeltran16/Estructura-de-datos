#ifndef WRITE_NUMBERS_H
#define WRITE_NUMBERS_H

/**
 * @brief Solicita al usuario una lista de números y los guarda en un archivo.
 *
 * Esta función permite al usuario ingresar una serie de números enteros que luego se almacenan en un archivo
 * especificado por el usuario. El proceso de entrada termina cuando el usuario ingresa el número -9999, 
 * el cual no se guarda en el archivo.
 */
void writeNumbersToFile();

#endif // WRITE_NUMBERS_H
